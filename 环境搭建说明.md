# 环境搭建与协作指南（搭档篇）

> 本文给**第二位开发者（搭档）**看。照着做，把你自己的电脑环境搭起来，和第一位开发者（meteor）一起开发。
> 最后更新：2026-08-03

---

## 〇、先说清楚"协作模式"

**代码通过 Gitee 同步，但数据和运行环境各自电脑独立。**

```
你电脑上的文件           Gitee 仓库              搭档电脑上的文件
  你改代码 ──push──→  ────  存最新版  ────pull──→  搭档改代码
                      ←──pull──  ←──push──────
```

- 开工前先 `git pull` 拿最新代码
- 改完 `git commit` + `git push` 上传
- **数据（MaxKB 知识库、题库数据库、API Key）不共享**，各自配置

---

## 一、需要安装的软件

| 软件 | 用途 | 下载地址 | 版本要求 |
|---|---|---|---|
| Git | 拉取/同步代码 | https://git-scm.com | 任意较新版本 |
| Docker Desktop | 运行 MaxKB（知识库） | https://www.docker.com/products/docker-desktop | 最新版 |
| Python | 跑后端 API | https://www.python.org/downloads | 3.13.x |
| Node.js | 跑前端门户 | https://nodejs.org | 20+（24 最佳） |

安装 Git 时一路默认即可。Python 安装时**务必勾选 "Add Python to PATH"**。

---

## 二、拉取代码

1. 用你的 **Gitee 账号**（让 meteor 在 Gitee 仓库里把你加成"开发者"后）
2. 打开命令行（PowerShell 或 CMD），运行：

```bash
git clone https://gitee.com/meteor0620/meteor.git AI教辅智学平台
cd AI教辅智学平台
```

> 若提示输入账号密码，就输你自己的 Gitee 账号密码。
> 仓库是私有的，只有被加为成员的人才能克隆。

---

## 三、配置敏感信息（必须）

**从 meteor 那里拿到 `后端\.env` 文件**（含 DeepSeek API Key、MaxKB 密码），放到 `后端\` 目录下。

`.env` 不会被 git 同步（已在 `.gitignore` 排除），**必须手动传递**（微信/QQ 发文件即可）。没有它，AI 生成题目、判卷、知识库代理都会失败。

---

## 四、安装依赖

```bash
# 后端依赖
cd 后端
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt   # 若无此文件，见下方说明

# 门户依赖
cd ..\门户
npm install
```

> 若 `后端` 没有 `requirements.txt`，按需安装：
> `pip install fastapi uvicorn pydantic`

---

## 五、搭建 MaxKB（最麻烦的一步）

MaxKB 跑在 Docker 里，需要**在你自己的电脑上重建**（数据不能从代码同步）。

### 5.1 启动 MaxKB

```powershell
# 启动 Docker Desktop（等它右下角图标变绿）
# 然后：
docker run -d --name=maxkb --restart=always -p 8080:8080 1panel/maxkb
```

> ⚠️ **国内网络拉镜像很慢**（Docker Hub 被墙）。需要配置镜像源（阿里云/DaoCloud），方法见 `工作记录.md` 的"运行环境"一节。

### 5.2 初始化 MaxKB

1. 浏览器打开 `http://localhost:8080`
2. 首次进入按提示设置管理员账号（**密码规则：6-20位，含大小写字母+数字+特殊字符**）
3. 登录后：系统设置 → 模型管理 → 添加 **DeepSeek** 模型（用 `.env` 里的 API Key）

### 5.3 建知识库（两个）

| 知识库 | 内容 | 数据来源 |
|---|---|---|
| 高等数学 | 4章136节微积分笔记 | `素材\高等数学微积分完整笔记.md` |
| 大学英语 | 英语讲义 | `素材\大学英语讲义.md` |

在 MaxKB「知识库」→ 新建知识库 → 上传文档 → 选对应的 md 文件 → 自动分段与向量化。

### 5.4 建应用（两个）并关联

- 应用「AI微积分助教」→ 关联数学知识库，模型选 DeepSeek
- 应用「AI大学英语助教」→ 关联英语知识库
- 配置知识库检索（搜索模式 blend、top_n 5、相似度 0.3）
- 每个应用生成 access_token，填到 `门户\src\App.vue` 的 `subjects` 数组

> 完整配置参数见 `工作记录.md` 的核心资源 ID 速查表和 prompt 经验。

---

## 六、启动平台

方式一：双击 `启动平台.ps1`（**注意**：脚本里写死了 meteor 的绝对路径 `C:\Users\meteo\...`，你自己电脑上请右键编辑，把两处路径改成你的实际路径，或手动分步启动）。

方式二：手动分步启动（推荐，避免路径问题）：

```powershell
# 1. MaxKB 已在 Docker 运行（5.1 完成）

# 2. 后端
cd 后端
.venv\Scripts\python -m uvicorn main:app --host 0.0.0.0 --port 8001

# 3. 门户（新开一个终端）
cd 门户
npm run dev
```

启动后访问：
- MaxKB 后台：http://localhost:8080
- 后端 API：http://localhost:8001
- 学生门户：http://localhost:5173

---

## 七、日常协作流程

```bash
# 1. 开工前：拉最新代码
git pull

# 2. 改代码...

# 3. 提交并上传
git add .
git commit -m "描述你做了什么"
git push
```

**建议分工（避免改同一文件冲突）**：
- meteor：后端 `后端\*.py`
- 搭档：门户 `门户\src\*`

**若 push 提示冲突**：先 `git pull`，手动合并冲突文件，再 `git add .` → `commit` → `push`。

---

## 八、常见坑速查

- 中文乱码：PowerShell 发中文 JSON 会乱码，用 Python 脚本以 UTF-8 发送
- 门户改了 vite 配置后必须重启（热更新不生效）
- 修改 MaxKB 应用后必须带 `knowledge_id_list` 重新 PUT 并 publish
- 前端公式渲染：DeepSeek 的 `\(...\)`/`\[...\]` 会自动转成 `$`/`$$`，无需手动处理

> 更多踩坑记录见根目录 `工作记录.md`（项目唯一交接文档，每次动手前先读）。
