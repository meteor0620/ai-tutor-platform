# 工作记录

> 2026-07-31 | 青岛理工大学 AI 应用创新开发大赛（教学赋能类）
> 最后更新：2026-08-03 凌晨（修复"泰勒问成洛必达"根因）
> 用途：本文件是项目唯一交接文档，新 session 先读本文件再动手。

---

# 一、当前状态总览（最重要）

**系统三件套全部运行中：**

| 服务 | 地址 | 账号/说明 |
|---|---|---|
| MaxKB 管理后台 | http://localhost:8081 | 账号/密码见本地 `.env`（不入库） |
| 后端 API (FastAPI) | http://localhost:8001 | 题库/组卷/判卷/知识库代理 |
| 门户 (Vue3) | http://localhost:5173 | 学生端界面 |

**核心资源 ID 速查表（改配置常用）：**

| 资源 | ID |
|---|---|
| DeepSeek 模型 | `019fbced-de0f-73e2-85e9-1e3f13b62d42` |
| 内置 embedding 模型 | `42f63a3d-427e-11ef-b3ec-a8a1595801ab` |
| 数学知识库 | `019fbcf6-bf8f-7471-ad2a-8c4dac17ed2c` |
| 数学应用 | `019fbcf9-161f-7810-9870-764741e2072a`（token: `81e652b7c938cdcb`） |
| 英语知识库 | `019fbd04-246b-7392-bc02-667d803a7f39` |
| 英语应用 | `019fbd04-73b1-7063-8b5e-99b42eb219dc`（token: `4ecd86138bcbf5ec`） |
| DeepSeek API Key | 见本地 `.env` 的 `DEEPSEEK_KEY`（敏感，不入库） |

---

# 二、平台架构（三层）

```
门户 Vue3 (:5173) ── vite proxy ──┬── /chat, /admin → MaxKB (:8080)   [AI对话/知识库]
                                  └── /api          → FastAPI (:8001)  [题库/组卷/判卷/错题/知识库代理]

FastAPI 后端 (:8001)：
  ├── questions.py   题库（手动录入 + DeepSeek批量生成）
  ├── papers.py      智能组卷/在线作答/自动判卷/错题本
  ├── knowledge.py   知识库代理（浏览/检索，内部持 admin token，不暴露）
  └── database.py    SQLite（aiplatform.db）
```

- 学生门户功能：科目选择（高数/英语）→ AI答疑 / 在线自测 / 知识库（书式三级目录）
- 题库现有 **81 题（数学）**，覆盖 15 个知识点（极限/导数/泰勒/洛必达/多元函数/微分方程/级数/定积分等），另含英语 15 题
- 知识库内容：数学=开源LaTeX笔记转Markdown（4章136节）；英语=AI生成讲义
---

# 三、立即要做的事 / 待办清单

**1.【已完成】"泰勒问成洛必达"问题根因与修复（2026-08-02 深夜）✅**
- **根因（真凶）**：MaxKB `base_search_dataset_step.list_paragraph` 用 `QuerySet(Paragraph).filter(id__in=...)` 按数据库默认顺序（插入序）返回段落，**丢失检索相似度排序**。注入 prompt 的段落顺序随机，模型被低相关段落（如"有理式的不定积分"）带偏，无上下文瞎答。
- **辅助根因 1**：应用 prompt **缺少 `{data}` 占位符**！`to_human_message` 用 `prompt.replace('{data}', data)`，无占位符则检索内容整个被丢弃 → 模型完全无上下文。
- **辅助根因 2**：`App.vue openSession` 自动发"你好"开场白 → "我没问你问题却答洛必达"。
- **修复动作**：
  1. 微积分/英语应用 prompt 均补上 `{data}` 占位符 + 段落选择规则（选标题与问题最匹配的 `<data>` 段，忽略无关段）
  2. `knowledge_setting` 改为 `search_mode: blend` + `top_n: 5` + `similarity: 0.3`（embedding 模式本地 text2vec 排序差且慢，已弃用）
  3. `temperature` 0.7 → **0.4**（降低随机性）
  4. `App.vue` 删除自动"你好"开场白消息
  5. `MarkdownRender.vue` normalizeFormulas 增加扫描器：跨行单 `$...$` 自动升级为块级 `$$...$$`（修复公式源码暴露）
  6. **`style.css` 移除 `.msg-text` 的 `white-space: pre-wrap`**（多轮对话公式错乱的元凶——pre-wrap 强制保留 KaTeX 公式内部空白/换行，导致 `0/0` 竖排、公式拆行）
- **验证**：泰勒→泰勒展开、洛必达→洛必达法则、定积分→定积分、拉格朗日→拉格朗日中值定理、导数→导数、极限→极限，多轮追问正常 ✅

**2. 调整 AI 回复风格（已完成）**
- 微积分/英语应用 prompt 已改为"精炼结构化讲解"风格（核心思想→公式→必考→易错→几何直观）
- **重要经验**：修改应用后必须 **PUT 更新时带上 `knowledge_id_list`**（否则知识库关联被清空，AI 会乱答）
- **重要经验**：修改后必须 **重新 publish** 才生效
- **重要经验**：prologue 已清空（设为空串），首问不再被开场白拦截
- **新坑：PUT 更新应用的 `model_id` 字段**！GET 返回键名是 `model`，PUT 需要 `model_id`。若漏传/传错会**把模型配置清空**（表现为"抱歉，AI 模型未配置"）。PUT 前必须 `model_id` 取自 GET 的 `model`，`stt_model_id`/`tts_model_id` 取自 GET 的 `stt_model`/`tts_model`

**3. 后续开发方向（按比赛优先级）**
- P0 闭环已通：组卷→作答→判卷→逐题解析→错题本 ✅
- 数学题库已扩至 81 题 ✅（组卷覆盖更广）
- 待做：教师端界面、AI授课计划、AI生成PPT、学情看板、英语题库扩充
- 技术路线：MaxKB为主 + 自研代码做亮点（自研前端/PPT脚本/学情脚本）

---

# 四、服务启动/停止（重要，避免卡死）

**⚠️ 卡死教训**：在 shell 会话内用 `Start-Process` + 输出重定向启动 uvicorn/vite 会导致会话卡死（后台服务持有输出句柄）。**永远不要这样操作**。

**正确启动方式**：
- 一键启动：双击/运行 `C:\Users\meteo\Desktop\AI教辅智学平台\启动平台.ps1`（启动 Docker→MaxKB→后端→门户）
- 后端单启动：`后端\start.bat`（注意 bat 中文注释有 GBK 乱码问题，仅英文可用）
- 门户单启动：`门户\start.bat`
- 停止后端：`后端\stop.bat`

**如果服务已经挂掉**，手动恢复顺序：
1. Docker Desktop → `docker start maxkb`（容器 `--restart=always` 会自动）
2. 后端：`cd 后端 && .venv\Scripts\python -m uvicorn main:app --host 0.0.0.0 --port 8001`
3. 门户：`cd 门户 && npm run dev`

---

# 五、MaxKB API 速查（改配置常用）

**登录/鉴权**：`POST /admin/api/user/login` body `{"username":"<admin账号>","password":"<见本地.env>","current_role":"ADMIN"}` → 拿 token，后续 `Authorization: Bearer <token>`

**公开对话全流程**（门户用，admin token 也可走 chat）：
1. `POST /chat/api/auth/anonymous` body `{"access_token":"<应用的access_token>"}` → chat token
2. `GET /chat/api/open` (Bearer chat token) → chat_id
3. `POST /chat/api/chat_message/<chat_id>` body `{"message":"...","stream":false,"re_chat":false}` → 回答

**⚠️ 注意**：`/chat_message/<id>`（无 `/api/`）会被前端 SPA 兜底，必须用 `/chat/api/chat_message/<id>`

**应用管理**：
- 查看应用：`GET /admin/api/workspace/default/application/<app_id>`
- 修改应用：`PUT /admin/api/workspace/default/application/<app_id>`（**必带 `knowledge_id_list`**，否则清空知识库关联）
- 发布应用：`PUT /admin/api/workspace/default/application/<app_id>/publish`（改完必须发布）
- 拿 access_token：`GET /admin/api/workspace/default/application/<app_id>/access_token`

**知识库**：
- 文档列表：`GET /admin/api/workspace/default/knowledge/<kb_id>/document`
- 段落分页：`GET .../knowledge/<kb_id>/document/<doc_id>/paragraph/<page>/<size>`
- 检索测试：`POST .../knowledge/<kb_id>/hit_test` body `{"query_text":"...","top_number":3,"similarity":0.2,"search_mode":"blend"}`
- 上传文档：`POST .../knowledge/<kb_id>/document` body `{"name":"...","paragraphs":[{"title":"..","content":".."}],"type":0}`（type 必须数字，0=BASE）

**后端 API（门户走 vite 代理，端口 8001）**：
- 题库：`GET/POST /api/questions`，AI生成 `POST /api/questions/generate`
- 组卷：`POST /api/papers`，取卷 `GET /api/papers/<id>`，提交 `POST /api/papers/<id>/submit`
- 错题本：`GET /api/wrong-book`
- 知识库浏览：`GET /api/knowledge/<subject>/documents`、`/documents/<id>/sections`
- 知识库检索：`POST /api/knowledge/<subject>/search` body `{"subject":"math","query":"...","top_n":5}`

---

# 六、关键经验与踩坑记录（新 session 必读）

## 运行环境
- Windows 11 家庭版 x64；Python 3.13.14；Node 24；Git 2.54
- 网络：手机热点（192.168.43.x），DNS 受限，Docker Hub 被墙
- Docker 镜像源（daemon.json 已配）：阿里云/DaoCloud/网易等，USTC 失效
- WSL2 内核就绪，无 Ubuntu 发行版（Docker Desktop 自带）

## Python 坑
- **Python 3.13 的 urllib 有兼容 bug**：`Request(url, method=...)` + `urlopen(req, body)` 会报 `_validate_method` 拿 dict。**用 requests 库**，别用 urllib 发请求
- 后端虚拟环境在 `后端\.venv`

## 中文乱码坑
- **PowerShell 5.1 发中文 JSON 会乱码**（GBK）→ 必须用 Python 脚本以 UTF-8 发送
- 终端显示中文乱码多数是编码显示问题，实际数据正常（用 Python 验证）

## 门户/Vue 坑
- **vite 修改 proxy 配置后必须重启**（热更新不生效）
- npm create vite 在中文路径下会创建到错误位置，需 Move-Item
- MarkdownRender 用 `markdown-it` + `markdown-it-texmath` + `katex`，**delimiters 用 dollars**（brackets 模式块级公式不识别）
- normalizeFormulas：DeepSeek 的 `\(...\)`/`\[...\]` → `$`/`$$`

## LaTeX→Markdown 转换坑（数学知识库）
- `\input` 展开时 `m.group(1)` 已含 .tex 后缀，**别再拼 .tex**（会变 xxx.tex.tex 导致内容全丢）
- 环境列表需包含：definition/theorem/lemma/proposition/corollary/example/remark/proof/principle/inference/property/statement
- **eqnarray 必须转 `\begin{aligned}`**（`& = &` 三列→`& =` 两列），否则 KaTeX 渲染失败显示原始代码
- figure 环境（多行）用 `re.sub(..., flags=re.DOTALL)` 删除
- cases 环境保留（分段函数，公式内合法）
- 带参数环境 `\begin{theorem}[带余除法]` 的标记正则要写成 `@@BEGIN\{(\w+)\}(\[[^\]]*\])?@@`

## 知识库内容来源
- 数学：GitHub 开源仓库 calculus-notes（已归档到 `D:\python-projects\GitHub\desktop-tutorial\calculus-notes`）
  - 原始位置：`C:\Users\meteo\Documents\GitHub\desktop-tutorial\calculus-notes`
  - 转换脚本产出的 md：`素材\微积分LaTeX\高等数学微积分完整笔记.md`（19万字，4章136节）
- 英语：AI 生成的 `素材\大学英语讲义.md`
- **注意**：开源仓库"多元函数的极限与连续性"章节原本就是空的（只有标题），非转换丢失

## 其他
- MaxKB 应用 prologue 已清空（避免首问被拦截）
- 数据库文件：`后端\aiplatform.db`（SQLite）
- 门户 subjects 配置在 `门户\src\App.vue`（加科目：MaxKB建库+应用 + subjects数组加一项）

---

# 七、项目背景与目标（简短）

青岛理工大学 AI 应用创新开发大赛，教学赋能类赛道。
- 学生端：知识库检索、AI讲解、智能自测、自动判卷、错题本
- 教师端：知识库管理、智能组卷、AI授课计划、AI生成PPT、学情看板
- 技术路线（已定）：MaxKB为主 + 自研代码做差异化亮点
- 排期：7月底—9月30日，当前约处于第一阶段末/第二阶段初

## 已完成的里程碑
1. Docker + MaxKB + DeepSeek 模型配置 ✅
2. 双科目知识库（数学完整开源笔记 + 英语讲义）✅
3. 首个应用 AI微积分助教，RAG 对话验证 ✅
4. Vue3 门户（科目选择 + AI答疑 + 在线自测 + 知识库书式浏览）✅
5. 自测闭环：组卷→作答→判卷→逐题解析→错题本 ✅
6. 公式渲染修复 + 知识库 eqnarray 转换 ✅
7. AI 回答风格调优（精炼结构化）✅
8. 修复"泰勒问成洛必达"根因：prompt 补 `{data}` 占位符 + MaxKB 段落排序 bug 规避 + 前端公式跨行修复 ✅
9. 数学题库扩充至 81 题（DeepSeek 批量生成，覆盖15知识点）+ 修复判卷总分 100 分制 bug ✅（待最终验证）

## 账号密码
- MaxKB：账号/密码见本地 `.env`（`MAXKB_ADMIN` / `MAXKB_PASSWORD`，不入库）
- 密码规则：6-20字符，必须含大小写字母+数字+特殊字符
